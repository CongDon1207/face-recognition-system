# Hướng dẫn chạy chương trình (Run)

## Yêu cầu trước khi chạy
Đảm bảo bạn đã kích hoạt môi trường ảo (Venv).

## Lệnh chạy
Tại thư mục gốc dự án:

```bash
# Windows (Git Bash / CMD)
./venv/Scripts/python.exe main.py
```

Hoặc nếu đã activate venv:
```bash
python main.py
```
